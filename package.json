{
  "name": "iana-timezones",
  "version": "1.1.2022e",
  "description": "Package to download IANA time zone database and convert to JSON array of time zone names",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "jsdom": "^20.0.1",
    "tar-stream": "^2.2.0",
    "typescript": "^4.8.4"
  }
}